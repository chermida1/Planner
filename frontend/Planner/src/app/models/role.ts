export class Role {
    public roleId: number;
    public roleType: string;

    constructor(roleId: number, roleType: string) {
        this.roleId = roleId;
        this.roleType = roleType;
    }
}
